import { Styled } from '@jxnblk/gatsby-theme-mdx-blog'
import { Link } from 'gatsby'
import Layout from '../layout'

export default Layout

This is my personal site while I learn GatsbyJS.

View the **<Styled.a as={Link} to='/blog'>Blog</Styled.a>** to see
it in action or view the <Styled.a as={Link} to='/unstyled'>Unstyled page</Styled.a>
to see how styles are isolated from the global scope.